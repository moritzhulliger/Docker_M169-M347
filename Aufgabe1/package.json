{
  "name": "container-test",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "esbuild app.js --bundle --outfile=dist/app.js --platform=node --format=cjs"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.4"
  }
}